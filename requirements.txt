# Core FastAPI and Web Server
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
pyodbc==5.1.0  # For SQL Server

# Security and Authentication
bcrypt==4.1.3
cryptography==42.0.8
keyring==25.2.1
passlib[bcrypt]==1.7.4

# Legacy App Dependencies (retained for now)
tkcalendar==1.6.1
requests==2.31.0
matplotlib==3.8.4
win10toast==0.9
Pillow==10.3.0

# General Utilities
packaging==24.0

# Testing (optional for runtime, but good for dev)
pytest==8.2.2
pytest-cov==5.0.0
requests==2.31.0 # For testing API endpoints
httpx==0.27.0 # Modern async HTTP client for testing FastAPI